<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cultura.mapper.MemberMapper">

    <select id="login" resultType="MemberVO">
    select member_id, passwd, nickname from member_tb where member_id = #{member_id} and passwd = #{passwd}    
    </select>

</mapper>